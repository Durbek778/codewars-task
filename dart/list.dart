void main() {
    
// adding number

// List<int> numbers = [1, 2, 3];
// List<int> increasedNumbers = numbers.map((number) => number + 5).toList();
// print(increasedNumbers); 

// taking some portion of each element

List<String> names = ["Alice", "Bob", "Charlie"];

List<String> initials = names.map((name) => name.substring(0, 2)).toList();
print(initials);



    

}